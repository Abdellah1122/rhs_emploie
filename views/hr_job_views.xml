<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!-- Minimalist Modern Kanban View -->
  <record id="rhs_job_kanban_minimalist_modern" model="ir.ui.view">
    <field name="name">hr.job.kanban.minimalist.modern.rhs_emploi</field>
    <field name="model">hr.job</field>
    <field name="inherit_id" ref="hr_recruitment.view_hr_job_kanban"/>
    <field name="arch" type="xml">

      <!-- Enhanced kanban attributes -->
      <xpath expr="//kanban" position="attributes">
        <attribute name="class">o_kanban_dashboard o_hr_recruitment_kanban minimalist_modern_kanban</attribute>
      </xpath>

      <!-- Add all required fields -->
      <xpath expr="//kanban" position="inside">
        <!-- Original core fields -->
        <field name="active"/>
        <field name="name"/>
        <field name="alias_email"/>
        <field name="is_favorite"/>
        <field name="department_id"/>
        <field name="no_of_recruitment"/>
        <field name="color"/>
        <field name="new_application_count"/>
        <field name="no_of_hired_employee"/>
        <field name="manager_id"/>
        <field name="user_id"/>
        <field name="application_count"/>
        <field name="all_application_count"/>
        <field name="activities_today"/>
        <field name="activities_overdue"/>
        <field name="company_id"/>
        <field name="alias_id"/>
        
        <!-- Custom enhanced fields -->
        <field name="request_date"/>
        <field name="deadline_date"/>
        <field name="status"/>
        <field name="experience"/>
        <field name="language"/>
        <field name="city"/>
        <field name="activity_domain"/>
        <field name="nbr_jours_restants"/>
        <field name="type_contrat"/>
      </xpath>

      <!-- Clean minimalist kanban card template -->
      <xpath expr="//t[@t-name='kanban-box']" position="replace">
        <t t-name="kanban-box">
          <div t-attf-class="#{!selection_mode ? kanban_color(record.color.raw_value) : ''} minimalist-card"
               style="background: #ffffff;
                      border: 1px solid #f3f4f6;
                      border-radius: 18px;
                      box-shadow: 0 2px 8px rgba(220, 38, 38, 0.04), 0 1px 3px rgba(0, 0, 0, 0.02);
                      transition: all 0.3s ease;
                      overflow: hidden;
                      position: relative;
                      margin: 24px;
                      min-height: 240px;">

            <!-- Clean status indicator with red accent -->
            <t t-set="status_value" t-value="record.status and record.status.raw_value or ''"/>
            <div t-attf-style="position: absolute;
                              top: 0;
                              left: 0;
                              right: 0;
                              height: 3px;
                              background: #{status_value == 'ouvert' ? '#dc2626' : 
                                          status_value == 'en_cours' ? '#f97316' : 
                                          status_value == 'cloture' ? '#6b7280' : 
                                          status_value == 'annule' ? '#991b1b' : '#dc2626'};"></div>

            <!-- Card content -->
            <div style="padding: 20px;">

              <!-- Header with favorite and urgency -->
              <div class="d-flex justify-content-between align-items-start mb-3">
                <field name="is_favorite" widget="boolean_favorite" nolabel="1" 
                       style="color: #dc2626; font-size: 1.1rem;"/>
                
                <t t-set="days_remaining" t-value="record.nbr_jours_restants and record.nbr_jours_restants.raw_value"/>
                <div t-if="days_remaining !== undefined" 
                     t-attf-class="urgency-pill #{days_remaining &lt; 0 ? 'overdue' : days_remaining &lt;= 7 ? 'urgent' : 'normal'}"
                     t-attf-style="padding: 4px 10px;
                                   border-radius: 12px;
                                   font-size: 0.7rem;
                                   font-weight: 600;
                                   background: #{days_remaining &lt; 0 ? '#fee2e2' : days_remaining &lt;= 7 ? '#fef3c7' : '#fef7f7'};
                                   color: #{days_remaining &lt; 0 ? '#dc2626' : days_remaining &lt;= 7 ? '#d97706' : '#dc2626'};
                                   border: 1px solid #{days_remaining &lt; 0 ? '#fecaca' : days_remaining &lt;= 7 ? '#fed7aa' : '#fecaca'};">
                  <t t-esc="record.nbr_jours_restants.value"/>j
                </div>
              </div>

              <!-- Job title -->
              <h3 style="color: #111827;
                         font-weight: 600;
                         font-size: 1.15rem;
                         line-height: 1.4;
                         margin: 0 0 12px 0;
                         letter-spacing: -0.025em;">
                <field name="name"/>
              </h3>

              <!-- Status badge -->
              <div class="mb-3">
                <span t-attf-style="display: inline-flex;
                                   align-items: center;
                                   gap: 6px;
                                   padding: 5px 12px;
                                   border-radius: 16px;
                                   font-size: 0.7rem;
                                   font-weight: 500;
                                   background: #{status_value == 'ouvert' ? '#fef2f2' : 
                                               status_value == 'en_cours' ? '#fff7ed' : 
                                               status_value == 'cloture' ? '#f9fafb' : 
                                               status_value == 'annule' ? '#fef1f1' : '#fef2f2'};
                                   color: #{status_value == 'ouvert' ? '#dc2626' : 
                                           status_value == 'en_cours' ? '#ea580c' : 
                                           status_value == 'cloture' ? '#6b7280' : 
                                           status_value == 'annule' ? '#991b1b' : '#dc2626'};
                                   border: 1px solid #{status_value == 'ouvert' ? '#fecaca' : 
                                                      status_value == 'en_cours' ? '#fed7aa' : 
                                                      status_value == 'cloture' ? '#e5e7eb' : 
                                                      status_value == 'annule' ? '#fca5a5' : '#fecaca'};">
                  <div t-attf-style="width: 5px;
                                     height: 5px;
                                     border-radius: 50%;
                                     background: #{status_value == 'ouvert' ? '#dc2626' : 
                                                 status_value == 'en_cours' ? '#ea580c' : 
                                                 status_value == 'cloture' ? '#6b7280' : 
                                                 status_value == 'annule' ? '#991b1b' : '#dc2626'};"></div>
                  <t t-esc="record.status and record.status.value or 'N/A'"/>
                </span>
              </div>

              <!-- Clean info grid -->
              <div style="display: grid; 
                          grid-template-columns: 1fr 1fr; 
                          gap: 12px 16px; 
                          margin-bottom: 20px;">
                
                <!-- Request Date -->
                <div>
                  <div style="color: #6b7280; font-size: 0.7rem; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px;">
                    Date demande
                  </div>
                  <div style="color: #111827; font-size: 0.8rem; font-weight: 500;">
                    <t t-esc="record.request_date and record.request_date.value or '-'"/>
                  </div>
                </div>

                <!-- Deadline -->
                <div>
                  <div style="color: #dc2626; font-size: 0.7rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px;">
                    Échéance
                  </div>
                  <div style="color: #dc2626; font-size: 0.8rem; font-weight: 600;">
                    <t t-esc="record.deadline_date and record.deadline_date.value or '-'"/>
                  </div>
                </div>

                <!-- Experience -->
                <div>
                  <div style="color: #6b7280; font-size: 0.7rem; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px;">
                    Expérience
                  </div>
                  <div style="color: #111827; font-size: 0.8rem; font-weight: 500;">
                    <t t-esc="record.experience and record.experience.value or '-'"/>
                  </div>
                </div>

                <!-- City -->
                <div>
                  <div style="color: #6b7280; font-size: 0.7rem; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px;">
                    Ville
                  </div>
                  <div style="color: #111827; font-size: 0.8rem; font-weight: 500;">
                    <t t-esc="record.city and record.city.value or '-'"/>
                  </div>
                </div>

                <!-- Language -->
                <div>
                  <div style="color: #6b7280; font-size: 0.7rem; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px;">
                    Langue
                  </div>
                  <div style="color: #111827; font-size: 0.8rem; font-weight: 500;">
                    <t t-esc="record.language and record.language.value or '-'"/>
                  </div>
                </div>

                <!-- Contract -->
                <div>
                  <div style="color: #6b7280; font-size: 0.7rem; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px;">
                    Contrat
                  </div>
                  <div style="color: #111827; font-size: 0.8rem; font-weight: 500;">
                    <t t-esc="record.type_contrat and record.type_contrat.value or '-'"/>
                  </div>
                </div>
              </div>

              <!-- User info -->
              <div class="d-flex align-items-center mb-3" style="padding: 10px; background: #fefefe; border-radius: 12px; border: 1px solid #f9fafb;">
                <div style="width: 28px; height: 28px; border-radius: 50%; background: linear-gradient(135deg, #fef2f2, #fee2e2); display: flex; align-items: center; justify-content: center; margin-right: 10px; border: 1px solid #fecaca;">
                  <i class="fa fa-user" style="color: #dc2626; font-size: 0.75rem;"></i>
                </div>
                <div>
                  <div style="color: #111827; font-size: 0.8rem; font-weight: 500;">
                    <field name="user_id"/>
                  </div>
                  <div style="color: #6b7280; font-size: 0.7rem;">Responsable</div>
                </div>
              </div>

              <!-- Email alias (if exists) -->
              <div t-if="record.alias_email.value" class="mb-3" 
                   style="padding: 10px; background: #eff6ff; border-radius: 12px; border: 1px solid #dbeafe;">
                <div style="display: flex; align-items: center; gap: 8px;">
                  <i class="fa fa-envelope-o" style="color: #2563eb; font-size: 0.75rem;"></i>
                  <div style="color: #2563eb; font-size: 0.8rem; font-weight: 500;">
                    <field name="alias_id"/>
                  </div>
                </div>
              </div>

              <!-- Actions and stats -->
              <div class="d-flex justify-content-between align-items-center">
                <!-- Main action button -->
                <button class="btn btn-primary" 
                        name="%(hr_recruitment.action_hr_job_applications)d" 
                        type="action"
                        style="background: linear-gradient(135deg, #dc2626, #b91c1c);
                               border: none;
                               border-radius: 12px;
                               padding: 10px 16px;
                               font-size: 0.8rem;
                               font-weight: 500;
                               transition: all 0.2s ease;
                               box-shadow: 0 2px 4px rgba(220, 38, 38, 0.2);">
                  <field name="new_application_count"/> Nouvelles
                </button>

                <!-- Statistics -->
                <div class="d-flex gap-12">
                  <div style="text-align: center;">
                    <div style="color: #dc2626; font-weight: 700; font-size: 1.1rem; line-height: 1">
                      <field name="no_of_recruitment"/>
                    </div>
                    <div style="color: #6b7280; font-size: 0.55rem; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px;">
                      À recruter
                    </div>
                  </div>

                  <div t-if="record.application_count.raw_value > 0" style="text-align: center; margin-left:1em">
                    <div style="color: #059669; font-weight: 700; font-size: 1.1rem; line-height: 1;">
                      <field name="application_count"/>
                    </div>
                    <div style="color: #6b7280; font-size: 0.55rem; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px;">
                      Candidatures
                    </div>
                  </div>
                </div>
              </div>

              <!-- Activities alert (if any) -->
              <div t-if="record.activities_today.raw_value > 0 or record.activities_overdue.raw_value > 0" 
                   style="margin-top: 12px; padding: 10px; background: #fef3c7; border-radius: 12px; border: 1px solid #fed7aa;">
                <div style="display: flex; align-items: center; gap: 8px;">
                  <i class="fa fa-exclamation-triangle" style="color: #d97706; font-size: 0.75rem;"></i>
                  <div style="color: #d97706; font-size: 0.8rem; font-weight: 500;">
                    <span t-if="record.activities_today.raw_value > 0">
                      <field name="activities_today"/> aujourd'hui
                    </span>
                    <span t-if="record.activities_overdue.raw_value > 0">
                      <field name="activities_overdue"/> en retard
                    </span>
                  </div>
                </div>
              </div>

            </div>

            <!-- Activity domain footer -->
            <div t-if="record.activity_domain.value" 
                 style="border-top: 1px solid #f3f4f6; padding: 12px 20px; background: #fefefe;">
              <div style="display: flex; align-items: center; gap: 8px;">
                <i class="fa fa-industry" style="color: #6b7280; font-size: 0.75rem;"></i>
                <span style="color: #6b7280; font-size: 0.8rem; font-weight: 500;">
                  <t t-esc="record.activity_domain.value"/>
                </span>
              </div>
            </div>

          </div>

          <!-- Minimalist CSS with red accents -->
          <style>
            .minimalist-card:hover {
              transform: translateY(-4px);
              box-shadow: 0 8px 25px rgba(220, 38, 38, 0.12), 0 4px 12px rgba(0, 0, 0, 0.04);
              border-color: #fecaca;
            }

            .minimalist-card .btn:hover {
              background: linear-gradient(135deg, #b91c1c, #991b1b) !important;
              transform: translateY(-1px);
              box-shadow: 0 4px 12px rgba(220, 38, 38, 0.3);
            }

            .minimalist_modern_kanban .o_kanban_record {
              margin: 24px !important;
            }

            .urgency-pill.overdue {
              animation: subtle-pulse 2s ease-in-out infinite;
            }

            @keyframes subtle-pulse {
              0%, 100% { opacity: 1; }
              50% { opacity: 0.8; }
            }

            .minimalist-card {
              backdrop-filter: blur(10px);
            }

            .minimalist-card:hover .urgency-pill {
              transform: scale(1.05);
            }
          </style>

        </t>
      </xpath>

    </field>
  </record>
</odoo>